data List a =
       | Nil
       | Cons a (List a)
last :: forall a. a -> List a -> a =
  \@a ->
    \(x0 :: a) ->
      \(xs :: List a) ->
        match xs with
        | Nil -> x0
        | Cons pm$1 pm$2 ->
          match pm$2 with
          | Nil -> pm$1
          | Cons x xs -> last @a x xs
